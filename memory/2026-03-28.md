# 2026-03-28

- Journal preference confirmed: if there was no work or the system wasn’t used (no boot/no activity), skip writing a growth journal entry for that day.
- 23:53 Alan said good night; wrote the 2026-03-28 growth journal entry.
- Added note: tried several browser connection methods; final successful connection is worth recording.
- Added a "難啃的技術" note covering browser connection troubleshooting, port and port+2 confusion, and the final fix of installing Chrome in WSLG.
- Set up two OpenClaw gateways: main on port 18789 and secondary on port 18792 (secondary configured as its own systemd service). Tokens were generated/updated for both (not storing raw tokens here).
- Secondary gateway bound to LAN (0.0.0.0) to allow Windows Chrome access; browser headless enabled.
- Tried to use Chrome extension relay with gateway=18792 (Plan B), but extension expects relay port = gateway+3 (18795). No relay service on 18795 in this build; relayBindHost config rejected. This caused token rejected/relay unreachable errors.
- Options discussed: (A) use gateway 18789 so relay is 18792 (default for extension), (B) run Windows node host so relay lives on Windows, or (C) use Chrome inside WSL.
