# 2026-06-21

- Weekly novel check for `chapter14.html` had only produced a placeholder review scaffold in `memory/novel-review/2026-06-21-chapter14-draft.md` (`第14章：待定`, `（待撰寫...）`), but `memory/novel-progress.json` had incorrectly advanced to `awaiting_novel_ok`.
- Alan replied `NOVEL OK` at `2026-06-21 08:11:52 GMT+8`; publish was intentionally blocked because `scripts/handle_weekly_novel_ok.sh` correctly detected the scaffold/placeholder state and refused to publish.
- Corrective action: reset `memory/novel-progress.json` to `status=needs_draft` so later checks reflect reality until a real chapter 14 draft is written and explicitly validated into `awaiting_ok`.
- Simplified weekly novel flow per Alan's decision: remove the placeholder scaffold step and the separate pre-OK ready-mark step from the normal path. New rule is direct real draft into `review_file`, then `NOVEL OK` triggers validation plus publish in one path.
- BWCamera 小修：橫拍照片先前會在 `File.toNormalizedBitmap()` 階段被 `ensurePortrait()` 強制轉回直式，導致最後輸出失去 landscape 方向，只能人工再轉 90 度。已改成 preview 保持直式正規化，但 capture/save 路徑保留原始橫直向；`./gradlew assembleDebug` 於 `2026-06-21` 本次 session build 成功，待 Alan 實機再驗。
- BWCamera 後續驗證顯示第一層修補還不夠：因為 `MainActivity` 在 manifest 仍鎖 `portrait`，`CameraApp.kt` 的 `Preview` / `ImageCapture` / `ImageAnalysis` 原本只在 bind 當下抓一次 `previewView.display.rotation`，橫拍時 capture 的 `targetRotation` 很可能仍停在直式。已補上 `OrientationEventListener`，讓三個 use case 隨實際裝置方向更新，且拍照當下再把 `imageCapture.targetRotation` 寫回一次；`./gradlew assembleDebug` 再次 build 成功，下一個必要訊號是 Alan 實機重拍橫幅照片確認。
- Alan 於 `2026-06-21 10:30 GMT+8` 回報最新 debug build 已實機驗證通過，直拍與橫拍都正常，表示這次 landscape orientation 問題已解。
- 依 Alan 要求，已把這個可用節點升版為 BWCamera `0.1.1`（debug 為 `0.1.1-debug`），並加上預覽區點擊指定位置後觸發 CameraX `startFocusAndMetering()` 的 tap-to-focus；`./gradlew assembleDebug` 於同次 session 再次成功，tap-to-focus 尚待實機手感驗證。
- BWCamera 新小修：Alan 回報 film grain 在大圖上看起來有規律、不夠 random。根因是 `MonochromeFilterEngine.kt` 原本只用線性的 `pixelIndex` 做單一路徑 hash，放大後容易長出掃描式 pattern。已改成以 `x/y` 座標為主的三層混合 grain noise（coarse/fine/micro）並加上亮部收斂，降低明顯重複感；`JAVA_HOME=$HOME/.local/jdks/temurin-17 ./gradlew assembleDebug` 於 `2026-06-21` 本次 session build 成功，待 Alan 實機看 grain 視覺是否自然。
- BWCamera 追加 UX 小修：Alan 於 `2026-06-21 12:55 GMT+8` 回報 tap-to-focus 實機對焦 OK，但缺少點擊回饋。已在 `CameraApp.kt` 的 preview overlay 補上使用者點擊位置的小對焦框，顯示約 1 秒後自動消失；`JAVA_HOME=$HOME/.local/jdks/temurin-17 ./gradlew assembleDebug` 再次成功。當前待驗證訊號分成兩個：對焦框手感，以及 film grain 新演算法在大圖上的隨機感是否足夠自然。
- Alan 於 `2026-06-21 13:00 GMT+8` 回報最新版已「好很多」，認為這版基礎功能完整、沒有大問題，接下來只需繼續試用觀察。依要求，這個節點應保存成可回退的 BWCamera 穩定版本，內容包含 tap-to-focus 視覺回饋小框與新版 film grain 演算法。
