# 2026-05-05

- 整理硬碟 workflow 正式落地，先把 Office 類任務產物統一收進 `work_tmp/`，不再和 root、說明文件、skill 或 source code 混在一起。
- 新增並落實 `work_tmp/tasks/`、`work_tmp/shared/`、`work_tmp/logs/`，也補了 `work_tmp/TEMP_POLICY.md` 說明暫存與產物的分類原則。
- Office 類腳本大量改路徑，包含 `slides/scripts/create_*.py`、`excel/scripts/create_*.py` 與 `scripts/send_*.sh`，並新增 `scripts/office_output_path.py` 作為共用 helper。
- 後續又把散落的暫存圖、captcha、scratch 腳本、OCR 輸出、Pixnet logs 收進 `work_tmp/` 對應子目錄，避免 root 目錄繼續長出一次性垃圾。
- Pixnet 這塊先做成可遷移結構：新增 `scripts/pixnet_paths.js`，讓 runtime 預設走 `work_tmp/pixnet-playwright-test/`，必要時才 fallback 到舊 `tmp/` 路徑。
- 將正式 Pixnet workflow 從 `tmp/pixnet-playwright-test/` 升格整理到 `scripts/pixnet/`，把 login、publish、republish、find/fix/check、controller、helper 等長期腳本抽成正式區。
- 補上 `scripts/pixnet/README.md`、`tmp/pixnet-playwright-test/README.md`，把正式 workflow、runtime data、debug sandbox 三者邊界寫清楚。
- 補 `.gitignore`，忽略 Pixnet browser profile、profile archive、controller status/command、runtime JSON 與 node_modules，避免把不該版控的執行期大檔再次納入 repo。
- 把一批明顯是一次性或過渡期的 Pixnet 腳本搬到 `tmp/pixnet-playwright-test/archive/`，先觀察幾天是否仍被依賴，再決定刪除。
- 這波整理的關鍵不是單純移檔，而是把「正式腳本 / 執行期資料 / debug 暫存」三種東西明確拆開，之後硬碟整理、找檔、判斷能不能刪都會輕鬆很多。
