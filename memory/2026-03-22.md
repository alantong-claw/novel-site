# 2026-03-22

- Verified Telegram channel is connected and Alan's direct Telegram session is `telegram:8707204748`; Telegram reminders can target that chat.
- Replaced the earlier weekly public-backup cron with a weekly Sunday 09:30 Telegram reminder asking Alan to mount Windows `G:` for backups. Current reminder job id: `2f9ae341-b14c-4415-86d9-5f441d2ddc34`.
- Backup destination naming was corrected from `clawnchan`/`clawchan` typo to the proper assistant name: `ClawChan`. Intended Windows path is `G:\ClawChan\`, WSL path is `/mnt/g/ClawChan/`.
- WSL was not auto-mounting Windows drives because `/etc/wsl.conf` has `[automount] enabled=false`; Alan manually mounted the real Windows `G:` drive and confirmed it works.
- Confirmed real `G:` drive contents are visible under `/mnt/g`, and test write succeeded at `/mnt/g/ClawChan/clawchan_write_test_3.txt` (visible from Windows Explorer).
- The Telegram confirmation flow was tested: cron reminder sent to Telegram, Alan replied `OK` in Telegram, and that reply was confirmed in the Telegram session log.
- Full-backup script `/home/alantong/ai-work/backup_clawchan.sh` needed Windows/WSL compatibility fixes for writing to drvfs mounts. It was changed from `rsync -a` to a Windows-safer mode using `rsync -rlD --inplace --no-times --no-perms --no-owner --no-group` to avoid `mkstemp`, `chgrp`, and mtime-setting failures on `/mnt/g/ClawChan/`.
- A full backup retry to `/mnt/g/ClawChan/` was launched after the script fix and was still running at compaction time.
- Weekly novel automation was moved from heartbeat to cron, and a startup catch-up hook was added so Sunday shell startup will run the weekly novel check if 08:00+ has passed and the week is still missing.
