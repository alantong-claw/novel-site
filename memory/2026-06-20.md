# 2026-06-20

- BWCamera debug 驗證完成：Alan 用 Windows `adb.exe` 直接安裝 WSL 路徑下的 `app-debug.apk`，並用 `com.alan.bwcamera.debug` 啟動，確認這次裝到的是正確 debug 包，不是前一天的舊 app。
- 使用者回報目前這版 OK，且 `rotate 0` 顯示方向正確。
- 這輪先前誤判了 APK 與 source 同步就等於手機測到新版；實際踩雷點是：
  - debug 包與舊正式包可並存，且原本 launcher 名稱一樣，容易點錯。
  - 設定面板原本預設收合，會讓 `Brightness` / `Film grain` 看起來像根本不存在。
- 已做的實際修正：
  - `app/src/debug/res/values/strings.xml` 新增 debug 專用 app 名稱 `BW Camera Debug`。
  - `app/src/main/java/com/alan/bwcamera/camera/CameraUiState.kt` 將 `showSettings` 預設改為 `true`，讓亮度與雜訊滑桿一開就看得到。
- 重新 build 後的 debug APK 時間戳為 `2026-06-20 07:35:05 +0800`。
- 當前使用者決策：BW Camera 目前功能先告一段落，下一步改做 Google Play 上架準備。
- 追加驗證：Alan 後來確認前一次安裝指令貼錯，實際上用 Windows `adb.exe` 安裝 `\\wsl.localhost\Ubuntu\home\alantong\ai-work\bwcamera\app\build\outputs\apk\debug\app-debug.apk`，再用 `adb shell monkey -p com.alan.bwcamera.debug -c android.intent.category.LAUNCHER 1` 啟動，這版功能正常。
- 最新已驗證狀態：
  - `Brightness` / `Film grain` 已可見且可用。
  - `rotate 0` 顯示方向正確。
  - BW Camera 當前開發先暫停在這個可工作的節點。
  - 下一個主題切換為 Google Play 上架準備。
- 之後又發現新 bug：預覽正常、拍照可觸發，但照片不能儲存。
- Root cause：`ImageCapture.takePicture(... OnImageCapturedCallback ...)` 回來的拍照 buffer 預設是 `JPEG`，但 `app/src/main/java/com/alan/bwcamera/camera/ImageProxyBitmap.kt` 的 `toNormalizedBitmap()` 只接受 `YUV_420_888`，所以 capture success 後反而回傳 `null`，後面的黑白處理與 `MediaStore` 存檔完全沒跑到。
- 已修正：`ImageProxyBitmap.kt` 補上 `ImageFormat.JPEG` 分支，直接從第一個 plane 解碼位元組成 bitmap，再沿用既有旋轉補償與直向正規化流程。
- 修正後已重新執行 `JAVA_HOME=$HOME/.local/jdks/temurin-17 ./gradlew assembleDebug`，於 `2026-06-20` 當次 session build 成功。
- Alan 實測修正版後回報「這版 OK」，並要求把目前 BWCamera 狀態正式存下來，同步更新 `novel_site` 的成長日誌與難啃技術文章，且確認 commit、push 與網頁端內容都完成。
- 後續小修需求：手動 `rotate` 補償值不只要當次有效，下次重新啟動 app 也要記住。已用 app 內 `SharedPreferences` 持久化 `rotationCompensationDegrees`，build 再驗一次成功。
- 新增 durable 入口 `memory/tasks/bwcamera-index.md`，把 BWCamera 的 repo 路徑、已驗證安裝/啟動方式、關鍵坑、公開技術文、關鍵 commit、以及 Google Play 下一步集中成單點入口，之後提到 BW Camera 先看這份即可接回。
